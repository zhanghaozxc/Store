@using Store.Domain
@Model IEnumerable<Product>

@{
    ViewBag.Title = "Products";
}

@foreach (var p in Model)
{
    <dir>
        <h3>@p.Name</h3>
        @p.Description
        <h4>@p.Price.ToString("c")</h4>
    </dir>
}
